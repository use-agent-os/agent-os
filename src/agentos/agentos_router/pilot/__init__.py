"""Pilot router — self-trained local router (MiniLM backbone).

Rev 4 (MiniLM-locked) of the Pilot router. This package hosts the bounded
feature builder and, in later tasks, the strategy / model / post-processing
layers. It deliberately ships nothing OpenSquilla-derived.
"""
